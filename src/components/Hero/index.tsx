import React from 'react';
import { HeroWrapper } from './style';

const Hero: React.FC = () => {
  return (
    <HeroWrapper>
      <h1>Incroyable, moderne, fluide & sans template.</h1>
    </HeroWrapper>
  );
};

export default Hero;
